all:
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge cblibc
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge binutils
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge tinycc
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge nasm
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge flex
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge make
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge muon
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge samurai
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge bzip2
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge xz
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge curl
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge fribidi
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge libass
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge LuaJIT
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge cairo
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge dav1d
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge dialog
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge expat
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge ffmpeg
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge file
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge htop
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge idesk
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge imlib2
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge jwm
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge less
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge libarchive
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge libev
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge libpng
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge libressl
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge mandoc
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge ncurses
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge nickle
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge nsxiv
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge rhash
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge scrot
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge sndio
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge tabbed
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge tmux
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge tzdb
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge uthash
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge vim
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge xlockmore
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge zsh
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge zsh-autosuggestions
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge zsh-syntax-highlighting
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge Nordic-darker
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge Vanilla-DMZ
	GUEST="${PWD}/DEST" REPODIR="${PWD}" SRCDIR="${PWD}/dist" pkg merge Win98SE
